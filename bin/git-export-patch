#!/bin/bash

j=0;
for i in `git rev-list --reverse "$@"`; do
	let ++j;
	f="`printf %04u.diff $j`";
	echo -en "[$f] ";
	git log -1 --abbrev-commit --pretty=oneline "$i" | cat;
	git log -1 -C -M -p --stat=72 --summary "$i" >"$f";
done;
